#include <bits/stdc++.h>
using namespace std;

void fun(int arr[], int n){
    int maj = 0;
    int check = arr[0];
   for(int i=0;i<n;i++){
     
   }
}

int main() {
    int n;
    cout<<"Enter the val of n: ";
    cin>>n;
    cout<<"Enter the elements: ";
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    fun(arr, n);
    return 0;
}